import { NgModule} from '@angular/core';
import {Assign_usernameModule} from "./assign_username/assign_username.module";

@NgModule({
    imports: [
        Assign_usernameModule
    ],

})
export class SetupModule {
}
